# TODO

Ranked recommendations from the 2026-07-02 design review, highest priority first. Reproduced verbatim.

1. **Add γ.** Define a grouped fold (aggregation over a relationship's inverse image / neighbor stencil) as a first-class operator; without it the farm interlude is unimplementable and "σ/⋈/π" is false advertising.
2. **Decide the recurrence question.** Recurrences need scan semantics; scans can't live behind the barrier. Give it an Open Questions entry and re-gloss the Fibonacci examples honestly.
3. **Split the hop.** Keep `rel.Comp` functional; add an explicit set hop with quantifiers and an effect-at-image duplicate rule.
4. **Confront identity.** Add the intensional/extensional boundary to Open Questions: how does a script say "the same bandit as last tick"? (Relationship components already store the answer; the surface can't reach it.)
5. **Fix Tier 1's math to what it can prove**: generable-vs-nominal keys plus the frame/unit check. Cut the equivariance criterion, the ∎, and "reduction is a projection." Consider parametricity for Tier 3 — it repairs the one-axis story.
6. **Fix Tier 2's two leaks**: state the law over the full record (diagonal action), and adopt value-only tie-breaking for rank write-backs. Retire Version A grade().
7. **Reposition**: "embedded query-and-command engine with console ergonomics," beside a general scripting language. Claim determinism/replay; claim totality-by-construction over the eBPF framing.
8. **Add Inform 7 and production systems to the lineage**; cut or earn the Lisp row; note Rete/differential dataflow against the なる register, then design the なる register (marker, schedule, conflict rule).
9. **Publish a precedence table and a grammar appendix** with desugarings into the one form; define statement-level barrier granularity; sigil enum symbols.
10. **Repair the broken snippets** (masked multiply, `0⌾`, ex19/ex26 indexing, spiral, board literal, Erlang guard, Ridge) — the verification discipline is the repo's best habit, and it only works if the verifications verify. In the Japanese doc: fix the に case-frame story (or scope the natural surface to the naru voice, which is the honest and more interesting claim), fix `@spawn`, cite Ikegami and Rubin.
