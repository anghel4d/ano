# TODO

This is the endgoal register for branch `feature-jp-lexer`, written as prompts for the model that picks the work up. Each numbered entry is a standalone prompt in three parts: I, the issue as it stands; D, the design conditions any solution must satisfy; O, the outcome that closes the entry. Endgoals only — process, line numbers, and verification order live in the plans each entry cites. Read before acting, in this order: CLAUDE.md, src/compiler.md (architecture of record, end-state present tense — keep every sentence true), REGFIX.md (the case/API patch, plan of record), DATAMODEL.md (the ladder — the semantics every entry must cohere with), INTERACTIVE.md (the author's notebook pages, transcribed). Baseline: commit 178172b, all 229 demos green under `--run`, 113 conjugate pairs byte-identical under `--emit`; build with the cc line in REGFIX.md (`make` is not on PATH). Standing law at every entry: the author commits, you never do; do not violate ano-language.md, ano_nihongo.md, or ano-sky.md; never resolve an open design question silently — surface the tradeoff and stop; the demos' STABLE seal is lifted (2026-07-08), but demo and registry revision moves only through entry 19's process — Fable-supervised Opus 4.8 subagents, never inline. NEXT.md is fully executed and NEXT-2.md's live remainder is absorbed as entries 7 and 12 here (its item 2 is ruled); both carry closed status lines — archive them with REGFIX.md when its last entry closes. PATCHES.md is the running snapshot ledger, Minecraft-style, and stays. This file's earlier live-items list is carried as entry 15 verbatim and its Closed ledger stands at the bottom; register entries that close move there keeping their numbers, so the live numbering has gaps by design.

## Patch A — the case contract and `as`

3. One comparator, everywhere a name resolves. I: the shipped fold is first-letter-only — too narrow to be the rule, too magical to be no rule — and 111 of 229 demos lean on folding, so the fold is the rule. D: ASCII letters fold, every other byte exact, kanji untouched by construction; values (`:Sym` symbols, sym column values, char glyph runs) never fold; zero fold-collisions exist in the corpus, so nothing re-resolves. O: `names_eq` at every name-resolution site in registry.c and at the emitter's def-name lookup (ruled 2026-07-08, see Closed: defs fold, and a def fold-collision with another def or a registry entry is an emit-time error); `GOLD` resolves the `gold` column (a miss yesterday); `:nord` still does not match `Nord`; all 229 pre-existing demos emit byte-identical BQN to the pre-patch snapshot. Read: REGFIX.md items 1-2 (site list with line numbers); INTERACTIVE.md — the panel draws the contract, `Gold` folding at the prompt while `NORD` and `Nord` stay distinct race values.

4. Load-time strictness, scoped within name kinds. I: once names fold, two declarations whose names collide under the fold are the same name, and the loader accepts them today. D: reject entry-vs-entry and alias-source-vs-alias-source collisions, exact duplicates included; the reserved check folds too, `Til` rejected like `til`; do NOT reject alias-vs-entry — entries outrank aliases, deterministic shadowing, and seven STABLE registries depend on it (`ja x x`). O: every negative fixture in REGFIX.md Verification step 4 holds by hand. Read: REGFIX.md item 2.

5. `as`, the name-alias arity. I: `ja` is the only name-alias directive and its spelling documents the Japanese surface; there is no surface-neutral way to alias a name. D: `as <word> <name>` carries the ja hop's exact semantics — one hop, no transitivity, outranked by real entries, source word reserved-checked; one alias table both directives fill, `ja` kept as a distinct spelling at the declaration site; deliberately distinct from the `alias` line kind, which stores a mask — a value, not a name. O: both directives resolve through one alias loop in `reg_find`, and GRAMMAR.md documents the as/alias distinction beside the .reg format. Read: REGFIX.md item 3.

6. The derived tag — Option B's missing spelling. I: a tag is legal three ways — stored (A, shipped), registry-derived (B), program def (C, shipped) — and B's denotation is shipped (δ_v ∘ col, the interned-sym compare) but its registry name is not; exactly one spelling is missing on the whole board. D: `as <word> <col> <value>`, the third arity — the word is a name and folds, the value is a value and never folds; B-tags are read-only as effect targets, since δ_v has no inverse on the complement — the write spells `race = :Nord` and a derived tag in effect position is an error naming the carrier column; emission goes through the existing sym/num compare paths, no new emitter machinery; reg_dump serializes it back as the same line. O: `as nord race Nord` loads, selects, and errors correctly, and entry 8's witness passes. Read: DATAMODEL.md (The tag, three ways; The delta, item 1), REGFIX.md item 3.

7. Witness s56 — the last two roles. I: proto and id are routed through `reg_role` but never exercised under a declared role anywhere in the corpus (PATCHES.md verification report, finding #2). D: each role proven load-bearing the way s54's are — delete the role line and a pin must fail; demo format per NEXT-2.md §8. O: an s56 pair under demos/9-nihongo/ — a spawn-from-proto into a kanji-named proto column, a set-hop membership over a kanji-named id column — wired into check-ano.sh, counts rising. Read: NEXT-2.md item 1.

8. Witness s57 — representation independence, running. I: DATAMODEL.md's central claim — downstream, nothing can tell a stored tag from a derived one — is prose, not a test. D: one program, `Nord & Gold > 50 , Gold += 500`, over the notebook's own world (Gold 400 600 200 150 221; Master 0→2 and 3→4, none elsewhere; races Nord Khajiit Nord Nord Orc); registry A declares `col nord bool 1 0 1 1 0`, registry B declares the race sym column plus `as nord race Nord`; identical expect pins; deleting the `as` line must fail. O: pin equality across A and B as a permanent regression test — the representation-independence law with a green check. Read: DATAMODEL.md (The delta, item 2), INTERACTIVE.md (the ladder walked on live data — that chart is this world).

9. Docs for patch A. I: src/GRAMMAR.md's Names policy and src/compiler.md's resolution paragraph still state the first-letter clause, which patch A retires. D: the whole case contract in one line — names case-insensitive (ASCII fold, non-ASCII bytes exact), values case-sensitive; the word-class boundary beside it — the closed grammar (keywords, .reg directive words, the emitter's contextual specials `index`/`x`/`y`/`char`/`row`/`prev`) exact lowercase, only the open vocabulary folds, entries shadowing specials under the fold with `col X` shadows `x` as the hand-witness (word class and witness confirmed by the author, 2026-07-08); the shadowing rule, the one-resolver role behavior (ruled 2026-07-08, see Closed), and the load rejections documented beside the .reg format; compiler.md stays end-state present tense with every sentence true of the binary; no other doc targets. O: the docs and the binary agree. Read: REGFIX.md, Docs.

## Patch B — the write half of the commit loop

10. reg_dump. I: the commit loop the author drew — read in, binary tables in-memory, write out staged, mv commit — has no write half in anoc, so saves have no implementation story. D: `--dump <path>` serializes everything `reg_load` reads, post-state values included; write `<path>.staged` then rename(2) over the target — the atomic commit, crash-safe by construction; load → dump → load → dump fixpoints byte-identically; a dumped world passes the same pins as the in-memory one. O: a save file is demonstrably a registry dump — the world is a column store, so persistence is a dump. Read: REGFIX.md item 5, INTERACTIVE.md (The panel and the commit loop).

## Named, not started — the author's explicit go required; do not build

11. The widened entry taxonomy. I: the notebook's taxonomy (columns, spaces, arrays, extern func, net), the `.anoreg` extension, enums, and the C API header are named in the plans but unbuilt. D: I/O enters through registration, never through syntax; extern func is `struct { ano_fptr, attributes }` — the landing pad for purity metadata, the Sky Registry's hook; enums are the closed value universe B-tags draw from and the checked value types of the C API; the C API is the authoritative layer and .reg one frontend of it. O: stays recorded until the go; when it comes, it lands as registry entries, not language forms. Read: REGFIX.md item 6, DATAMODEL.md delta item 4, INTERACTIVE.md (the entry taxonomy; the registry, three layers).

12. The unspaced reader. I: the declared next tier of the Japanese surface. D: segment by maximal munch, but resolve munched spans against the registry after lexing, never during — the dictionary is a resolution-time oracle, the lexers stay registry-blind, two skins keep one parser. O: gated; the constraint is recorded here so it is not lost. Read: NEXT-2.md item 3, ano_nihongo.md (the post-branch note).

13. The ano-ecs.md derived-tag descriptor. I: the store blueprint's registry sketch (§10) has no descriptor for B-tags. D: proposed for the author beside §10, never performed — ano-ecs.md is his voice. O: a proposal shown, his edit. Read: DATAMODEL.md delta item 3.

14. The ano editor. I: the debug panel the notebook asks for — "get me a harness for this lang!". D: it lives in anoptic_engine, not this repo; entries 3-10 are everything it needs from this side. O: permanently out of scope here; INTERACTIVE.md holds the picture. Read: INTERACTIVE.md.

15. **Consider shipping proven-affine compilation.** Every statement is affine and alias-free by grammar, so the polyhedral arsenal (fusion, tiling, vectorization, parallelization) applies to all statements with no legality analysis. A backend that proves the schedule and emits raw column instructions is the Groq/TPU bet scoped to ano's fragment; the same property gives the CUDA lowering for free. Grading and lineage: `ano-sky.md`, "The speed claim, graded". (Added 2026-07-05.) Carried verbatim from this file's earlier live-items list; it coheres with the oracle condition below — the affine backend is a property of the real compiler (ano-ecs.md §14's plan VM), not a rewrite of anoc.

## Admissible, not adopted — the author's call, recorded not resolved

16. `Gold + 500` for `Gold += 500`. Effect position determines the write-back and the plan IR keys deltas by op class, so the `=` in `+=` carries zero information — the contraction is pure surface: terser, against the explicit read `+=` gives a reviewer, with the asymmetry that plain `=` has no contracted form. Read: DATAMODEL.md (Admissible, not adopted), INTERACTIVE.md (the flagship line's own margin: "or just +, can be inferred analytically").

17. Sparse tag storage. Row lists against bitmaps — same denotation, a storage class for the C store, already an open question at ano-ecs.md §16; no .reg change implied, no action.

## The spec's open questions — never resolve silently

18. Carried in ISSUES.md, all still open: identity across the barrier (extensional data outliving the intensional surface); recurrences within a statement (the wand cast pointer); quotation past the literal (the analyzable middle between eval's parse-time splice and the interpreter); order-srel derivation (fibers from a declared ground order); world-to-chunk frames — which DATAMODEL.md locates precisely as a missing change-of-basis form, the ladder's leftmost arrow without a surface; frame-homogeneous rule ticks; the clock as the install runs. Unsettled design beyond these lives in the spec's "Open Questions, Next Steps". D for all of them: locate any answer on the ladder first, check ano-ecs.md for the already-designed mechanism, then surface the tradeoff to the author. Read: ISSUES.md, DATAMODEL.md (The ladder).

## The demo revision — the corpus serves the semantics; sanctioned 2026-07-08

19. Demo and registry revision, by fan-out only. I: the STABLE seal is lifted — every demo compiles and passes, but passing is not truth: some were written against the compiler as it was, not the language as it is now ruled (the case contract, registry-as-API, the ladder); the author likes the vast majority, and the exceptions must change to fit the true vision, with the registries made more coherent alongside. D: semantics first — a demo changes only when the old one encodes wrong semantics, never for style; every change is declared per-demo, naming the ruling or doctrine it now conforms to, before it is made; execution is never Fable inline — fan out Opus 4.8 subagents (`model: opus`) with Fable supervising: Fable writes each per-demo brief, reviews every diff, and owns the result; conjugate pairs move together so emit-identity holds; check-ano.sh green after every batch, counts never dropping; the byte-snapshot discipline inverts here — every diff must match a declared intent, and an undeclared diff is a bug. O: a corpus where every demo and registry witnesses the ruled semantics, delivered as a per-demo change ledger plus the full diff, shown to the author to commit. Sequencing: after patch A, so revision targets the corrected resolution semantics. Read: REGFIX.md (the rulings), DATAMODEL.md (the ladder the demos must witness).

## Standing conditions — true at every entry above

- The Hierarchy: The Mathematics > The Semantics > The Grammar > The Syntax > keywords, pipelining, implementation details.
- Registry-as-API: complexity belongs registry-side, never as language quirks; the registry is one API with three layers, .reg one frontend of the future C struct ABI; the registry stores data — name-column-value triples are data — never programs (the B/C boundary).
- Denotation over representation: a noun denotes a mathematical object, the registry chooses its representation, and legality is compositional along the ladder, not enumerated — ano is mathematically defined, not rigidly engineered.
- One AST, one parser, one emitter; two lexer skins, both registry-blind; `ja` and `as` are resolution-level.
- anoc is the permanent v000001 oracle — 4,753 lines, BQN-emitting, differential-tested. It does not get rewritten; the real compiler is ano-ecs.md §14's plan VM over the C store, a separate future codebase tested against anoc. Flat whole-program builds; no caching or incremental machinery, ever.
- Verification is the byte: snapshot `--emit` of all 229 demos before the first edit and diff after — any undeclared diff on a pre-existing demo is a bug in your change (entry 19 is the one place diffs are declared first); check-ano.sh green; then show the author the full diff and STOP.

## Closed

Closed 2026-07-08, keeping their register numbers: entry 1, def-name folding — ruled as recommended, defs fold under the same comparator and a def fold-collision with another def or a registry entry is an emit-time error; implementation rides entry 3 and REGFIX.md item 1. Entry 2, the role resolver — ruled one-resolver, `reg_role`'s fallback calls `reg_find` with the declared `role` line as the explicit override; implementation rides REGFIX.md item 4, the doc line rides entry 9. Entry 20, work-tracking hygiene — applied (PATCHES.md heading dated and its commit line amended, NEXT.md and NEXT-2.md status-lined closed) and promoted to a one-line CLAUDE.md convention; needs no live entry.

The 2026-07-02 design review's ten items all landed, verified by both suites green (80 .bqn witnesses, 105 .ano twins): γ as the grouped fold (§13), the recurrence entry and the honest Fibonacci gloss (Open Questions; §21), the split hop with quantifiers and the idempotent image rule (§5), the identity entry (Open Questions), the Tier 1 regenerable-key math and Tier 3 parametricity, the Tier 2 diagonal action and value-only rank ties, the repositioning (Technical Explanation: query-and-command engine, determinism/replay, totality over the eBPF framing), the lineage rows (Inform 7, OPS5/CLIPS, Rete note; Lisp row cut) and the designed なる register (§11), the precedence table and grammar appendix with desugarings, barrier granularity, and the enum sigil, and the snippet repairs plus the nihongo fixes (ゼロが/する case frame scoped to the voice split, Ikegami and Rubin cited).
