# 00 — open rulings: the residue, Q/A

Everything ruled in the 2026-07-11 review is recorded in the task files and is not re-asked here. These are the eight decision points that arose DURING the review's investigation and have no recorded answer yet — each consuming task flags them "surface at execution", so answering here unblocks the agents in advance. Fill the A-lines; the consuming task reads this file first.

Q1. The registered archetype construct's name. You floated `def`; flag raised after: program-side `def` already means derived columns and standing rules, so the pun crosses the registry/program boundary. `proto` is currently a magic role name (the spawn glyph column), so it half-exists. Candidates: proto, arch, kind, def regardless. → consumed by 02.
A1.

Q2. None's surface spelling, post-collision. You proposed `/`; flag raised after: `/` already carries fold-marker and dyadic replicate — this would be its third meaning on one glyph. The ¯1 sentinel stays registry-side either way. Candidates: `/` anyway, a `none` keyword, no surface literal at all. → 02 and the spec's Open Questions.
A2.

Q3. When a world registers several `unique` columns, which one mints on spawn. Candidates: first declared, an explicit marker, at most one unique per world (refuse more). → 02.
A3.

Q4. The `^` seam. Demo 14 pins both `!Whiterun` and `!^Whiterun` for one registered bind. (a) `^` reserved to host-resolved deictics, binds always bare — matches the grammar appendix, demo 14 loses one spelling; or (b) `^` as optional constant-reference sugar on any registered constant — both spellings legal forever, the appendix paragraph rewritten. → 05.
A4.

Q5. The ja word for the 08 faction→leader rename. Proposed: 頭領. Alternatives: 指導者, or your own. → 03.
A5.

Q6. The fate of `reduce(` once `fold(` lands: deprecated alias for one cycle, or clean cut. → 05.
A6.

Q7. Close-out on `>/`. Recorded verdict: max/ and max\ stay named, `>/` rejected — folding `>` is non-associative, and making it mean max is the same reinterpretation pun refused for `-/`; k earns `|/` because k's `|` IS max natively. Accept, or overrule. → 04.
A7.

Q8. The seven migrated play copies (`.kore/play/` copies of 1-selection/{01,03,06,08}, 2-effects/{12,s10a,s10b}) holding your hand edits from before the corpus restore: review each at 03's execution, adopt all, or discard all. Reminder: `>reset` wipes them — rule this before resetting. → 03.
A8.
