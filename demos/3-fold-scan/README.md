# fold-scan

The active witnesses are 028–032, 038–040, and 139–142. Demos 033–037 await the spatial rewrite owned by `99`.

- 028: reductions over live alias scopes, the identity/no-row split on the empty scope, and the partition law under `!^`.
- 029: `fold(+)` and the exact ordered steps — subtraction and a registered non-associative accumulator, where the fold is the last prefix.
- 030: every admitted fold's running form, the mean's prefixes, `#\` over admitted rows, and the empty scan against the zero count.
- 031: `scan(f) … along` a declared traversal order, beside the same descriptors over row order.
- 032: the mean machine — state (sum,count), prefix emission, the finish as the last prefix, and the empty scope's no-row answer.
- 038: `|\` is the ever-any latch.
- 039: `&\` is the still-all latch.
- 040: a registered reducer supports `threat/`, `fold(threat)`, and `threat\`.
- 139: accumulation order, where the left recurrence and a right fold genuinely disagree.
- 140: the same recurrence at three magnitudes, including one column wholly inside the subnormals.
- 141: integers beside fractions, where every prefix rounds and the fold-equals-last-prefix law does the pinning.
- 142: Greater and Lesser over the char carrier, their bridges, and the empty glyph scope.
