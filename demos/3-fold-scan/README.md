# fold-scan

The active witnesses are 038–040. Demos 028–037 await the Greater/Lesser, accumulator, empty-result, and recurrence rewrites.

- 038: `|\` is the ever-any latch.
- 039: `&\` is the still-all latch.
- 040: a registered reducer supports `threat/`, `fold(threat)`, and `threat\`.
