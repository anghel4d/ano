-- Native world kinds, char half (s53c), ASCII twin: a kanji char column read per glyph by a kanji-named fn with a verbatim BQN body — the callable and column mangle together.
--! registry ../registries/s53-native-kinds-c.reg
--! expect 得点 = 0 1 0 0 1 0

刻印(印) , 得点 += 1
