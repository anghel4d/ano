-- Derived tag (s57a), option A of DATAMODEL.md's tag-three-ways: nord is a stored bool column and `Nord` folds to it. (was s57-derived-tag-a)
-- Here nord is stored. The same program and pins run against 132's derived registry, testing both representations on this case.
--! registry ../registries/131-derived-tag-a.reg
--! expect gold = 900 600 700 650 221
--! expect master = 2 -1 -1 4 -1

Nord & Gold > 50 , Gold += 500
