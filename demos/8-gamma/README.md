# gamma

Demos 079–081 are the live grouped-fold witnesses. Demos 076–078 are decommissioned.

- 079–080: `@` produces one scoped-global scalar while a tick-marked hop produces one fold result per source fiber.
- 081: identity-bearing folds keep empty fibers and identityless folds drop their result row.
