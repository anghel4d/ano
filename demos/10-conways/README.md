# conways

Conway's Game of Life (B3/S23) as a witness suite: what Life demands, the language already contains — a registered Moore srel (fibers shrink at the boundary, so the border is dead; a torus is a different fiber table, not a different program), the γ count over its fibers, mask algebra in a value write, and the barrier. The step is one statement; the flora act it out when the same line moves to the naru hinge. The cN names are Conway witnesses, not ano-examples.md rows.

- `105-life-step.bqn` — one synchronous generation: the fused value write (`Planted = kin == 3 | Planted & kin == 2`) and the Kin-snapshot triple agree with the hand-stepped blinker, either order of the ± pair; the barrier is Life's double buffer, so the in-place bug cannot be written in one statement; the stencil count is asserted equal to the γ fold over the moore fibers; period 2 back to the seed.
- `107-life-glider.bqn` — the flipbook: one statement per generation, four repetitions walk the glider one cell down-right, population pinned at 5 — a Tier-1 figure walking a conserved ground, no key minted, dropped, or moved. Performed it is one generation per statement; installed, one per tick.
- `108-life-naru.bqn` — the rule barrier is semantics: bloom/wither under §11's shared barrier equal the fused one-rule form in either commit order; giving each rule its own barrier (bloom commits, wither re-gathers) breaks the blinker in both orders, and differently. The runnable twin installs the one-rule fused form; the `-b` twin runs the two-rule pair under the shared barrier the emitter now implements, and the per-rule breakage stays pinned in the .bqn as the counterexample.

Both surfaced items landed (the guard-complement clause is recorded beside §11 in ano-language.md). The shared rule barrier: adjacent installs share one tick — every mask gathers the one pre-state, every effect stages into one commit set, the set scatters once. The admissibility certificate: bloom and wither both write planted with no common merge family, and a column-level footprint check cannot pass them; the emitter reads the complementary guard literals (`!Planted` / `Planted` over one pre-state) as the row-disjointness proof — §11's third clause. A same-column pair with neither a merge law nor complementary guards is rejected at emit time.
