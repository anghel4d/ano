# gamma

Gamma, the grouped fold over the fibers of a tick-marked relationship hop (`fold/ rel'.Comp`, `fold/ (rel' & pred)`), per the decision record.

- s11-gamma-def.bqn — the primitive: ids group vals, fold each fiber, align to targets; identity folds keep empty fibers at the identity, avg drops the row.
- 36-farm-gamma.bqn — the three rewritten ex36 farm lines: per-pen headcount, crops spread, per-plot moisture stencil mean with edge and corner cells; shift form cross-checked against the grouped-fiber form.
- 36-global-vs-gamma.bqn — the defect fixed: old §11 semantics broadcast one scalar into every pen, gamma gives the per-pen column; the two are asserted to differ.
