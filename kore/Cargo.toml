[package]
name = "kore"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "kore"
path = "src/main.rs"

[dependencies]
nix = { version = "0.31", default-features = false, features = ["fs", "ioctl", "poll", "process", "signal", "term"] }
